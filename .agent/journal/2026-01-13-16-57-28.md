# 2026-01-13 16:57:28

What changed, why:
- Keep the attendee polling interval running during active polls so the UI clears when a poll is closed.

What was observed:
- Tests not run (client-side polling tweak).

Commits:
- a99db1c Keep attendee polling active
