# Journal Entry (2026-01-12-17-10-23)

## What changed, why
- Added `clearAllPolls` in `lib/pollService.ts` to remove the active poll, its votes, and history.
- Added `POST /api/admin/clear` route with admin key validation to trigger the reset.
- Added admin UI button and confirmation to clear all polls.
- Added Jest setup and unit tests for `clearAllPolls`.
- Updated README manual validation steps to mention the clear-all action.

## What was observed
- `npm test` passes (2 tests).

## Links to commits
- No commits yet.
